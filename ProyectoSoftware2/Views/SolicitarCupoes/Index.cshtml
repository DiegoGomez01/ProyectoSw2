@model IEnumerable<ProyectoSoftware2.Models.SolicitarCupo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.materia.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CodEstudiante)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NombreEstudiante)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fecha)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.materia.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CodEstudiante)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NombreEstudiante)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
