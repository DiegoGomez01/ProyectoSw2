@model IEnumerable<ProyectoSoftware2.Models.PreRequisito>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PENSUM)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CODIGOMATERIA)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CODIGOMATERIA_PRE)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PENSUM)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CODIGOMATERIA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CODIGOMATERIA_PRE)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
